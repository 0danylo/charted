name: trend_notes
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  fl_chart: ^0.67.0
  flutter:
    sdk: flutter
  provider: ^6.1.2
  syncfusion_flutter_charts: ^25.1.41+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
